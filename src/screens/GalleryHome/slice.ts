// import { createSlice, PayloadAction } from "@reduxjs/toolkit"

// interface GalleryListState {
//     galleryList: Array<{}>
// };

// const initialState: GalleryListState = {
//     galleryList: [],
// };

// const galleryListSlice = createSlice({
//     name: "galleryList",
//     initialState,
//     reducers: {
//         setList(state, action: PayloadAction<{ galleryList: GalleryListState['galleryList'] }>) {
//             state.galleryList = action.payload.galleryList;
//         }
//     }
// });

// export const { setList } = galleryListSlice.actions;
// export default galleryListSlice.reducer;